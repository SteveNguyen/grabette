# This is a REFERENCE file — do NOT copy directly to /boot/firmware/cmdline.txt.
# The PARTUUID is specific to each SD card.
#
# Instead, edit your existing /boot/firmware/cmdline.txt and REMOVE:
#   console=serial0,115200
#
# The file must remain a SINGLE LINE (no line breaks) or the Pi will not boot.
#
# Example (your PARTUUID will differ):
console=tty1 root=PARTUUID=xxxxxxxx-xx rootfstype=ext4 fsck.repair=yes rootwait quiet splash plymouth.ignore-serial-consoles cfg80211.ieee80211_regdom=FR
